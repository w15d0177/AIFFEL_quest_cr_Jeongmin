/*import 'package:flutter/material.dart';
import 'catPage.dart';
import 'dogPage.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(initialRoute: '/', routes: {
      '/': (context) => CatPage(), // 고양이 페이지
      '/dog': (context) => DogPage(), // 멍멍이 페이지
    });
  }
}
*/